# Windows 전체 이식 · 단계 14

2026-09-16. 원격 main `2aaf0532e48016b32571c7929984a8f474e812ee`를 다시 확인했다. Apple Swift 소스는 바꾸지 않고 Windows 브랜치에서 작업했다. 단계 13에서 확인한 라이브러리 기능 누락을 구현했다.

## 동작

- 최근 삭제된 항목에서 **영구 삭제…**를 누르면 같은 WPF 외관의 확인 창이 열린다. 기본 버튼은 취소다. 확인한 녹음의 삭제 상태·오디오 버전·수정 ID를 다시 검사하고, 재생/파형 읽기를 닫은 뒤 삭제한다.
- 녹음 폴더의 오디오·전사·회의록·분석·수정 이력, 해당 녹음으로 식별되는 동기화 송신 복사본과 완료된 트랜잭션의 이전 파일을 지운다. 공유 수정 이력 수신함과 그 이력에서는 해당 녹음의 항목만 제거한다. 다른 회의의 데이터는 유지한다.
- 삭제 메타데이터는 바이트 그대로 유지하며 수정 시각/ID를 새로 만들지 않는다. `.purged`는 이 기기의 표시용 표식이다. UI는 숨기지만 동기화는 tombstone을 계속 전달한다. 삭제 도중 실패하면 `.purge-pending`을 남겨 다음 목록 로드에서 재시도한다.
- 삭제 후 30일이 지난 항목은 시작/목록 갱신 때 같은 정리를 수행한다. 파일이 잠겨 있으면 경고를 표시하고 다음 로드에서 다시 시도한다. 연결된 외부 폴더는 삭제 전 검사에서 거부한다.
- 다른 기기에서 복원하면 오디오와 문서를 다시 받으며, 로컬 삭제 표식 제거와 수정 이력 cursor 재설정을 같은 트랜잭션으로 적용한다. 수정 이력을 다시 받아 사용자가 바꾼 참여자 이름·프로젝트도 복원한다. 영구 삭제 상태에서 새 이력이 도착해도 로컬 파일을 다시 만들지 않는다.
- **오디오 내보내기…**는 저장된 WAV를 바이트 그대로 복사한다. 임시 파일에 복사 후 교체하며 취소/실패 시 기존 목적지를 보존한다. 라이브러리 내부와 연결된 외부 경로를 목적지로 삼지 않는다. 녹음 화면과 우클릭 메뉴에서 사용할 수 있다.

## 현재 확인한 증거

- 핵심 테스트: 30일 경계, 삭제 tombstone/다른 녹음 보존, 중단 후 재시도, 오래된 확인 거부, junction 차단, WAV 바이트 보존, 내보내기 취소와 목적지 잠금 실패.
- 실제 로컬 Worker HTTP/SQLite: 처음 동기화하기 전에 영구 삭제해도 tombstone 전송 성공. 삭제 후 다른 기기에서 복원하면 오디오·회의록·참여자 이름·추가 프로젝트 수정 복원 성공.
- 이 과정에서 오디오/문서만 복원하고 기존 참여자 수정 이력을 놓치는 문제를 재현했다. 복원 시 수정 이력 cursor를 되돌려 재수신하도록 수정했다.
- WPF 실행 `Windows/artifacts/ui-step14/library-files.json`: 실제 확인 창의 취소/확인, 확인 도중 다른 곳에서 복원한 녹음 보호, 열린 재생 리더 해제, 파일 내보내기 버튼, 영구 삭제 후 목록/숫자/앱 재시작 확인. 확인 창과 휴지통 화면을 렌더링해 직접 확인했다.
- 전체 회귀와 최신 self-contained 패키지 결과는 아래에 확정 기록한다.

## 검증의 경계

공개 서버나 사용자 라이브러리를 삭제하지 않았다. 테스트는 별도로 만든 녹음 폴더와 생성한 WAV만 사용했다. 저장 경로 선택은 WPF 검증에서 콜백으로 주입했으므로 Windows 운영체제의 Save 대화상자 조작 증거는 아니다. 삭제는 앱 파일 정리이며 디스크 보안 삭제나 서버 자산 삭제가 아니다. 외부 백업/내보낸 파일과, 이전 비정상 종료로 녹음 ID 없이 남은 임시 파일까지 찾아 지우는 기능은 포함하지 않는다. 실제 회의 품질, 실제 장치/Apple 기기/배포 서버와 전체 최종 배포 검증은 기존 전체 계획에 따라 남아 있다.
