<Window x:Class="NoteTaker.Windows.TranscriptImportWindow"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:c="clr-namespace:NoteTaker.Windows.Components" Title="전사문 가져오기" Width="680" Height="650" MinWidth="540" MinHeight="500"
 WindowStartupLocation="CenterOwner" ShowInTaskbar="False" WindowStyle="None" Style="{StaticResource {x:Type Window}}">
 <WindowChrome.WindowChrome><WindowChrome CaptionHeight="44" ResizeBorderThickness="6" GlassFrameThickness="0" CornerRadius="10" UseAeroCaptionButtons="False" /></WindowChrome.WindowChrome>
 <Border Background="{DynamicResource WindowSurface}" BorderBrush="{DynamicResource Separator}" BorderThickness="1" CornerRadius="10">
 <Grid><Grid.RowDefinitions><RowDefinition Height="44" /><RowDefinition Height="Auto" /><RowDefinition /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
  <Grid Background="{DynamicResource ToolbarSurface}"><c:WindowControls HorizontalAlignment="Left" WindowChrome.IsHitTestVisibleInChrome="True" /><TextBlock Text="전사문 가져오기" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center" /></Grid>
  <StackPanel Grid.Row="1" Margin="24,20,24,12"><TextBlock x:Name="RecordingTitle" FontSize="19" FontWeight="SemiBold" />
   <TextBlock Text="클로바노트에서 내려받은 TXT·SRT를 선택하거나 전사문을 붙여넣으세요. 기존 전사문은 이력에, 가져온 파일은 원문으로 보관합니다." Foreground="{DynamicResource Muted}" Margin="0,9,0,14" />
   <WrapPanel><Button Content="파일 선택…" Click="ChooseFile_Click" Margin="0,0,8,0" /><Button Content="텍스트 붙여넣기" Click="PasteMode_Click" /></WrapPanel>
   <TextBlock x:Name="SourceDescription" Text="아래에 전사문을 붙여넣으세요." Foreground="{DynamicResource Muted}" FontSize="11" Margin="0,10,0,0" />
  </StackPanel>
  <TextBox Grid.Row="2" x:Name="PreviewBox" AcceptsReturn="True" TextWrapping="Wrap" VerticalContentAlignment="Top" VerticalScrollBarVisibility="Auto" Margin="24,0,24,12" Padding="15" AutomationProperties.Name="전사문 미리보기" />
  <Border Grid.Row="3" BorderThickness="0,1,0,0" BorderBrush="{DynamicResource Separator}" Padding="24,14"><StackPanel>
   <TextBlock x:Name="ErrorText" Foreground="{DynamicResource RecordRed}" Margin="0,0,0,8" />
   <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"><Button Content="취소" IsCancel="True" Margin="0,0,8,0" /><Button x:Name="ConnectTranscriptButton" Content="이 녹음에 연결" Click="Import_Click" Style="{StaticResource PrimaryButton}" /></StackPanel>
  </StackPanel></Border>
 </Grid></Border>
</Window>
